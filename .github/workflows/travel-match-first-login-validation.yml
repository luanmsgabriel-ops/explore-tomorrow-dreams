name: Travel Match First Login Validation
on:
  pull_request:
    paths:
      - 'src/pages/MyTomorrowWelcome.tsx'
      - 'src/pages/ClientLogin.tsx'
      - 'src/lib/travelMatch.ts'
      - 'src/App.tsx'
      - '.github/workflows/travel-match-first-login-validation.yml'
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install --no-audit --no-fund
      - name: TypeScript
        run: npx tsc --noEmit
      - name: Scoped ESLint
        run: npx eslint src/pages/MyTomorrowWelcome.tsx src/pages/ClientLogin.tsx src/lib/travelMatch.ts src/App.tsx
      - name: Production build
        run: npm run build
      - name: Diff check
        run: git diff --check origin/${{ github.base_ref }}...HEAD
