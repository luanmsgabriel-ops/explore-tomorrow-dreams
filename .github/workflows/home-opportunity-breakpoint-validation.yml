name: Home Opportunity Breakpoint Validation

on:
  pull_request:
    branches: [main]
    paths:
      - 'src/components/landing/HomeOpportunityShowcase.tsx'
      - '.github/workflows/home-opportunity-breakpoint-validation.yml'
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install dependencies
        run: npm install --legacy-peer-deps
      - name: TypeScript
        run: npx tsc --noEmit
      - name: ESLint scoped
        run: npx eslint src/components/landing/HomeOpportunityShowcase.tsx
      - name: Build
        run: npm run build
